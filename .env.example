# AiScrum Pro — Environment Variables
#
# Copy this file to .env and fill in the values.
# The config loader substitutes ${VAR} references in .aiscrum/config.yaml.

# ntfy.sh push notification topic (required if ntfy notifications enabled)
NTFY_TOPIC=your-ntfy-topic-here
